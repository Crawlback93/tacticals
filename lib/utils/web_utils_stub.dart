/// Non-web stub — does nothing.
void openPopupWindow(String url) {}

/// Non-web stub — does nothing, returns no-op remover.
void Function() registerBeforeUnload(bool Function() hasUnsavedChanges) => () {};
